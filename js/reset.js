function showkelilingpersegi(flag) {
	document.getElementById('result-keliling-persegi').style.display = flag === 1 ? 'block' : 'none';               
}

function showluaspersegi(flag) {
	document.getElementById('result-luas-persegi').style.display = flag === 1 ? 'block' : 'none';               
}

function showluaspersegipanjang(flag) {
    document.getElementById('result-luas-persegipanjang').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilingpersegipanjang(flag) {
    document.getElementById('result-keliling-persegipanjang').style.display = flag === 1 ? 'block' : 'none';
}

function showluassegitiga(flag) {
    document.getElementById('result-luas-segitiga').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilingsegitiga(flag) {
    document.getElementById('result-keliling-segitiga').style.display = flag === 1 ? 'block' : 'none';
}

function showluasjajargenjang(flag) {
    document.getElementById('result-luas-jajargenjang').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilingjajargenjang(flag) {
    document.getElementById('result-keliling-jajargenjang').style.display = flag === 1 ? 'block' : 'none';
}

function showluastrapesium(flag) {
    document.getElementById('result-luas-trapesium').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilingtrapesium(flag) {
    document.getElementById('result-keliling-trapesium').style.display = flag === 1 ? 'block' : 'none';
}

function showluaslayanglayang(flag) {
    document.getElementById('result-luas-layanglayang').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilinglayanglayang(flag) {
    document.getElementById('result-keliling-layanglayang').style.display = flag === 1 ? 'block' : 'none';
}

function showluasbelahketupat(flag) {
    document.getElementById('result-luas-belahketupat').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilingbelahketupat(flag) {
    document.getElementById('result-keliling-belahketupat').style.display = flag === 1 ? 'block' : 'none';
}

function showluaslingkaran(flag) {
    document.getElementById('result-luas-lingkaran').style.display = flag === 1 ? 'block' : 'none';
}

function showkelilinglingkaran(flag) {
    document.getElementById('result-keliling-lingkaran').style.display = flag === 1 ? 'block' : 'none';
}